import { LoginForm } from "@/features/auth/components/login-form";

export const metadata = {
    title: "Login - MSG POS",
    description: "Masuk ke Sistem Point of Sale Swalayan",
};

export default function LoginPage() {
    return <LoginForm />;
}
